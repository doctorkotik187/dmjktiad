services:
  dmjktiad:
    image: ghcr.io/doctorkotik187/dmjktiad:latest
    container_name: dmjktiad
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - ./secrets.edn:/app/resources/secrets.edn:ro
    environment:
      - PORT=3000
      - HTTP_HOST=0.0.0.0
